# Python packages needed to build FionaDocs (html + pdf)
#   python3 -m venv .venv --prompt fionadocs
#   source .venv/bin/activate
#   pip install -r requirements.txt
sphinx==9.1.0
sphinx_rtd_theme==3.1.0
sphinxcontrib-mermaid==2.1.1
