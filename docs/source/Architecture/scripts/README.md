# Setupup documentation of system somponents

In order to create the proper documentation we need symbolic links to all the system scripts in this directory.

Use the ``setup.sh`` tool to create them with:

```bash
./setup.sh
```
